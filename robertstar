pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Thank You!"'
                sh '''
                    echo "Bruce"
                    ls -lah
                '''
            }
        }
    }
}
